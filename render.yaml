services:
  - type: web
    name: secret-speech-translator
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: TRANSCRIBE_MODEL
        value: gpt-4o-mini-transcribe
